- @question.answers.each do |answer| 
  = answer.body
  - if current_user && answer.user_id == current_user.id
      = button_to 'Delete answer', question_answer_path(@question, answer.id), method: :delete, class: 'button btn btn-danger btn-xs' 